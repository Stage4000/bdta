# Max-level analysis with a baseline to prevent regressions without blocking on existing issues.
parameters:
  level: 9
  paths:
    - backend/cron
    - backend/includes/workflow_helper.php
  parallel:
    maximumNumberOfProcesses: 4
includes:
  - phpstan-baseline.neon
