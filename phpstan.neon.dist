# Running at level 9 (highest) with a baseline to prevent regressions while existing findings are grandfathered.
parameters:
  level: 9
  paths:
    - backend/cron
    - backend/includes/workflow_helper.php
  parallel:
    maximumNumberOfProcesses: 4
includes:
  - phpstan-baseline.neon
